# mariaknoll.website

```bash
npm install

git clone --single-branch --branch newBranch https://github.com/buchervanesa/mariaknoll.website.git

git switch -c nameBranch
git checkout -b newBranchChange


# Fusionar

# Asegúrate de estar en la rama main:
git checkout main
# Actualiza main con los últimos cambios del repositorio remoto:
git pull origin main
# Fusiona newBranch en main:
git merge newBranch

git push origin main



```

```bash
# .env
# "watch": "node --watch --env-file=.env app.js"

MONGO_URI=mongodb://localhost:27017/tu_base_de_datos
USER1=fgp555
PASSWORD1=password___

```
